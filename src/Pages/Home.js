import React, {Fragment} from 'react';
import TopMenu from "../Components/TopMenu";

function Home(props) {
    return (
        <Fragment>
            <TopMenu/>
        </Fragment>
    );
}

export default Home;